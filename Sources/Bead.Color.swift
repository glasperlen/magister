import Foundation

extension Bead {
    public enum Color: Int, Codable, CaseIterable {
        case
        blue,
        pink,
        red,
        orange,
        green,
        yellow,
        teal,
        purple,
        indigo
    }
}
