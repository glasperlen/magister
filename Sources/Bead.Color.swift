import Foundation

extension Bead {
    public enum Color: String, Codable, CaseIterable {
        case
        red,
        orange,
        green,
        yellow,
        teal,
        purple,
        indigo
    }
}
